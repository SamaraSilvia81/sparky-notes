[package]
name = "sparky-notes"
version = "0.1.0"
description = "App desktop de captura e processamento de ideias com IA local"
edition = "2021"

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-global-shortcut = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[build-dependencies]
tauri-build = { version = "2", features = [] }
